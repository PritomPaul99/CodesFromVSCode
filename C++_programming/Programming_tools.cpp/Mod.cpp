#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a, b, c;

    cout << "Enter 1st of (a % b): ";
    cin >> a;
    cout << "Enter 2nd of (a % b): ";
    cin >> b;
    c = a % b;
    cout << "Mod is: " << c << endl;

    return 0;
}
